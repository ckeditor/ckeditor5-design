'use strict';

import Feature from 'ckeditor5-core/feature.js';
import Image from './image.js';

export const requires = [ Image ];

export default class ImageCaption extends Feature {
	constructor( editor ) {
		super();

		console.log( 'ImageCaption.constructor()' );
		console.log( 'Image should already be initialised', editor.features.get( Image ) );
	}
}