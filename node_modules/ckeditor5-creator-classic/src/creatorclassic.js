'use strict';

import Link from 'ckeditor5-link/src/link.js';

export default class CreatorClassic {
	constructor( editor ) {
		console.log( 'CreatorClassic.constructor()' );

		this.editor = editor;

		// Checking if cross-package imports work...
		this.editor.features.set( 'link', new Link() );
	}
}