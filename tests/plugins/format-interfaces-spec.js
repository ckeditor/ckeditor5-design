/* jshint browser: false, node: true, strict: true, mocha: true */

'use strict';

const chai = require( 'chai' );
const expect = chai.expect;
const formatInterfaces = require( '../../jsdoc/plugins/longname-fix/formatters/format-interfaces' );

describe( 'Long name fix plugin - format interfaces', () => {
	// TODO
} );